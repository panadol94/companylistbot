version: '3.8'

services:
  bot-platform:
    build: .
    container_name: list_company_bot_saas
    restart: always
    environment:
      - MOTHER_TOKEN=${MOTHER_TOKEN}
      - DB_FILE=/data/bot_platform.db
      - DOMAIN_URL=${DOMAIN_URL}
      - MEDIA_DIR=/data/media
      - GROQ_API_KEY=${GROQ_API_KEY}
    volumes:
      - ./data:/data
      - ./data/backups:/data/backups
      - ./media:/data/media
    ports:
      - "8085:8000"
